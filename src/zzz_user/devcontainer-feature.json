{
  "name": "User",
  "id": "user",
  "version": "0.0.1",
  "description": "Add a non-root user",
  "options": {
    "username": {
      "type": "string",
      "default": "contend",
      "description": "User name (string)"
    },
    "docker_group": {
      "type": "boolean",
      "default": true,
      "description": "Add the user to the docker group (boolean)"
    }
  }
}