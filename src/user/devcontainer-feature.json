{
  "name": "User",
  "id": "user",
  "version": "0.0.1",
  "description": "Add a non-root user",
  "options": {
    "username": {
      "type": "string",
      "default": "md",
      "description": "User name (string)"
    },
    "uid": {
        "type": "int",
        "default": "1000",
        "description": "User ID (int)"
    }
  }
}